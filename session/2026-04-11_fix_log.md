# 📋 Nhật ký Công việc — Phiên làm việc 2026-04-11 (Update)

## Thông tin phiên
- **Ngày:** 2026-04-11 13:54 (UTC+7)
- **Nhiệm vụ:** Sửa toàn bộ 26 bugs đã phát hiện trong hệ thống Bluebee LMS
- **Trạng thái:** ✅ Hoàn thành — 0 TypeScript errors

---

## Tổng kết bugs đã sửa

| Bug | Mức độ | File | Hành động đã thực hiện |
|---|---|---|---|
| C-01 | Critical | databaseService.ts | Thêm cảnh báo bảo mật + note cho dev chuyển sang Cloud Function |
| C-02 | Critical | geminiService.ts, documentProcessor.ts | `process.env.API_KEY` → `import.meta.env.VITE_GEMINI_API_KEY` |
| C-03 | Critical | Chatbot.tsx | Lọc welcome message + giới hạn 20 tin nhắn trong history |
| C-04 | Critical | ExamRoom.tsx | Fix timer closure bằng `viewModeRef` + loại viewMode khỏi deps |
| C-05 | Critical | ExamRoom.tsx | Xóa blur listener (double-count) + loại `timeLeft` khỏi deps |
| H-01 | High | AIGeneratorTab.tsx | Truyền `contextText` đúng là tham số 4th của `generateQuestionsByAI` |
| H-02 | High | examEngine.ts | Fix regex strip prefix - chỉ strip khi có space sau dấu phân cách |
| H-03 | High | databaseService.ts | Return `docs.length` thay vì `response.total` sau filter |
| H-04 | High | databaseService.ts | Validate ID thực sự tồn tại trong DB trước khi update |
| H-05 | High | App.tsx | Thêm try-catch QuotaExceeded, lưu KB per-doc có kiểm soát |
| H-06 | High | databaseService.ts | `Query.notEqual(null)` → `Query.isNotNull()` |
| H-07 | High | databaseService.ts | `Query.equal([classId, 'all', ''])` → `Query.or([equal(classId), equal('all')])` |
| H-08 | High | AuthContext.tsx | Xử lý 404 race condition khi xóa profile chờ trong register flow |
| M-01 | Medium | geminiService.ts | Implement fallback model thực sự: PRIMARY → FALLBACK_MODEL |
| M-02 | Medium | geminiService.ts | Fix `contents` string → array object format cho evaluateOralAnswer |
| M-03 | Medium | geminiService.ts | Giới hạn context 600K chars, thêm truncation warning |
| M-04 | Medium | ExamCreator.tsx | `Record<number, AnswerEntry>` → `Record<string, AnswerEntry>` |
| M-05 | Medium | Documents.tsx | Thêm keyboard navigation (Arrow keys) cho PDF viewer |
| M-06 | Medium | QuestionGenerator/index.tsx | Dùng `fetchQuestionMetadataForMatrix` thay vì fetch 5000 docs |
| M-07 | Medium | SelfStudy/index.tsx | Xóa import `fetchStudentAttemptCount` không sử dụng |
| M-08 | Medium | documentProcessor.ts | Xóa import `GoogleGenAI` không sử dụng |
| M-09 | Medium | App.tsx | Sửa key `elearning_docs` → `knowledge_base` đúng |
| L-01 | Low | Chatbot.tsx | Xóa import `Link` không sử dụng |
| L-02 | Low | Chatbot.tsx | useCallback + thêm `getInitialMessage` vào useEffect deps |
| L-03 | Low | ExamRoom.tsx | `NodeJS.Timeout` → `ReturnType<typeof setTimeout>` |
| L-04 | Low | Documents.tsx | Xóa prefix check cứng `cloud_`, dùng logic `!id || startsWith('temp_')` |
| M-10 | Medium | ExamCreator.ts, databaseService.ts | Fix updateExam không serialize `updates.config`, gây mất cấu hình header đề thi khi chỉnh sửa. |
| M-11 | Medium | OnlineTest/index.tsx | Kiểm tra `latestAttempt` chưa nộp trước khi check `max_attempts` để học viên có thể làm tiếp đề đang thi dở. |
| M-12 | Medium | examEngine.ts | Sửa lại logic `generateExamPaper` bị hardcode trộn câu/đáp án, cho phép nhận `shuffleQ`, `shuffleO` settings. |
| M-13 | Medium | OnlineTest, SelfStudy | Truyền `exam.shuffle_questions`, `exam.shuffle_options` vào `generateExamPaper`. |
| L-05 | Low | databaseService.ts, types.ts | `mapDbExamToLocal` return `Exam` (thêm fields còn thiếu vào interface) |

---

## Kiểm tra & Xác nhận

```
npx tsc --noEmit → 0 errors ✅
26/26 bugs đã sửa ✅
```

## Ghi chú Bảo mật

**C-01 (Server API Key)** - Đây là lỗ hổng bảo mật nghiêm trọng nhất nhưng cần migration lớn:
- **Hiện tại**: Đã thêm cảnh báo rõ ràng, key chỉ đọc từ env var (không hardcode)
- **Cần làm tiếp**: Chuyển `createAuthUserAsAdmin` và `deleteAuthUserAsAdmin` sang **Appwrite Cloud Function** hoặc một backend API proxy riêng
- **Deadline khuyến nghị**: Trước khi deploy production

---

## Bước tiếp theo

- [ ] Test manual: AI Question Generator với PDF thật
- [ ] Test manual: ExamRoom với học viên thật
- [ ] Migrate admin operations sang Appwrite Cloud Function (C-01 hoàn chỉnh)
- [ ] Monitor Gemini fallback model behavior trên production
